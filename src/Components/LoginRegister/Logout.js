import "./Logout.css"


function Logout () {

    const handleClick = () => {
   
    }

    return (
        <button className="logout-button" onClick={handleClick}>Logout</button>
    )
}

export default Logout;